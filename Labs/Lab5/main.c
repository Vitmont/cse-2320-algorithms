// n  is number of vertices not exceeding 100

struct edge
{
    int start, end, directed;
};

int main(int argc, char **argv)
{
    int vertices, edges;
    scanf("%d", &vertices);
    edges = (vertices * 2) - 3;
    //make a graph instance
    //int *edges = malloc(sizeof(int) * n)
    int i;
    for(i = 0; i < edges; i++)
    {

    }
}

